<h1><%= @user.username %></h1>

<%= image_tag(@user.avatar_url(:big)) if @user.avatar? %>

<h2>Goals</h2>

<% if !@user.goals.blank? %>
  <% for goal in @user.goals %>
    <%= goal.name %>
  <% end %>
<% else %>
  
<% end %>
